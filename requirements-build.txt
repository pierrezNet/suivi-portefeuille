# Dépendances de BUILD uniquement (pas requises au runtime de l'app).
# Installées en plus de requirements.txt sur le runner GitHub Actions
# windows-latest pour produire le .exe :
#   pip install -r requirements.txt -r requirements-build.txt
pyinstaller==6.11.1
